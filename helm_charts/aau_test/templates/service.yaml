apiVersion: v1
kind: Service
metadata:
  name: {{ include "aau_test.fullname" . }}
  labels:
    {{- include "aau_test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aau_test.selectorLabels" . | nindent 4 }}
